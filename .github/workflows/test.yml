name: Tests

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run test suite
      run: npm install -g yarn # Extra Step
      run: |
        yarn
        yarn test
